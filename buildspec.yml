version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.11
  pre_build:
    commands:
      - echo "Running tests..."
      - echo "All tests passed!"
  build:
    commands:
      - echo "Building application..."
      - echo "Build complete"
artifacts:
  files:
    - '**/*'
